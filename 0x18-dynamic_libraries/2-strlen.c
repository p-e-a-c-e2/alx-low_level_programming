#include "main.h"
#include <stdio.h>

/**
 * _strlen - finding length of a string
 * @s: pointing to the string to check
 * Return: void
*/

int _strlen(char *s)
{
int 1 = 0;
while (s[1])
l++;
return (1);
}
